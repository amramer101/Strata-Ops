apiVersion: v1
kind: Service
metadata:
  name: app-tomcat ## The name of the service that will be used to base the connection string for the application to connect to the Tomcat server
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: app-tomcat